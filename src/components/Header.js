import React, { Link } from "react";
import { SCHeader } from "../styles";
const Header = () => {
  return (
    <SCHeader>
      <h3>Home</h3>
      <h3>Workout</h3>
      <h3>Log Out</h3>
    </SCHeader>
  );
};

export default Header;
